@model RSDSystem.Models.Employee
@{
    ViewData["Title"] = "Add Employee";
    var jobs = ViewBag.JobClassifications as string[] ?? Array.Empty<string>();
}


<div class="content-topbar">
    <button class="bell-btn"><i class="bi bi-bell-fill"></i></button>
</div>

<a asp-action="Index" class="btn-back">
    <i class="bi bi-arrow-left"></i> Back
</a>

<div class="form-card">
    <form asp-action="Create" method="post" enctype="multipart/form-data">
        @Html.AntiForgeryToken()

        <!-- First Name | Last Name | M.I. -->
        <div class="form-row form-row-3">
            <div>
                <label class="form-lbl">First Name</label>
                <input type="text" name="FirstName" class="form-ctrl" required />
            </div>
            <div>
                <label class="form-lbl">Last Name</label>
                <input type="text" name="LastName" class="form-ctrl" required />
            </div>
            <div>
                <label class="form-lbl">M.I</label>
                <input type="text" name="MiddleInitial" maxlength="5" class="form-ctrl" />
            </div>
        </div>

        <!-- Date of Birth | Age | Gender -->
        <div class="form-row form-row-3b">
            <div>
                <label class="form-lbl">Date of Birth</label>
                <input type="date" name="DateOfBirth" id="dobInput"
                       class="form-ctrl" onchange="calcAge()" />
            </div>
            <div>
                <label class="form-lbl">Age</label>
                <input type="text" id="ageInput" class="form-ctrl" readonly placeholder="—" />
            </div>
            <div>
                <label class="form-lbl">Gender</label>
                <select name="Gender" class="form-ctrl">
                    <option value="">Select</option>
                    <option value="Male">Male</option>
                    <option value="Female">Female</option>
                </select>
            </div>
        </div>

        <!-- Address -->
        <div class="form-row form-row-1">
            <div>
                <label class="form-lbl">Address</label>
                <input type="text" name="Address" class="form-ctrl"
                       placeholder="Barangay, Municipality/City, Province" />
            </div>
        </div>

        <!-- Email | Contact Number -->
        <div class="form-row form-row-2">
            <div>
                <label class="form-lbl">Email</label>
                <input type="email" name="Email" class="form-ctrl" />
            </div>
            <div>
                <label class="form-lbl">Contact Number</label>
                <input type="text" name="ContactNumber" class="form-ctrl" placeholder="09XXXXXXXXX" />
            </div>
        </div>

        <!-- Assigned to Project -->
        <div class="form-row form-row-1">
            <div>
                <label class="form-lbl">Assigned to Project</label>
                <select name="ProjectId" class="form-ctrl">
                    <option value="">Select project</option>
                    @foreach (var p in (ViewBag.Projects as List<RSDSystem.Models.Project> ?? new()))
                    {
                        <option value="@p.ProjectId">@p.ProjectName</option>
                    }
                </select>
            </div>
        </div>

        <!-- Job Classification | Rate per Day | Rate per Hour -->
        <div class="form-row form-row-eq3">
            <div>
                <label class="form-lbl">Job Classification</label>
                <select name="JobClassification" class="form-ctrl" required>
                    <option value="">Select</option>
                    @foreach (var j in jobs)
                    {
                        <option value="@j">@j</option>
                    }
                </select>
            </div>
            <div>
                <label class="form-lbl">Rate per Day</label>
                <input type="number" name="DailyRate" class="form-ctrl"
                       step="0.01" min="0" placeholder="0.00" />
            </div>
            <div>
                <label class="form-lbl">Rate per Hour</label>
                <input type="number" name="RatePerHour" class="form-ctrl"
                       step="0.01" min="0" placeholder="0.00" />
            </div>
        </div>

        <!-- Photo -->
        <div class="form-row form-row-1">
            <div>
                <label class="form-lbl">Photo</label>
                <div class="file-row">
                    <label class="file-choose-btn" for="photoInput">Choose File</label>
                    <span class="file-name-lbl" id="fileLabel">No File Chosen</span>
                </div>
                <input type="file" id="photoInput" name="photo" accept="image/*" class="d-none"
                       onchange="document.getElementById('fileLabel').textContent =
                           this.files.length ? this.files[0].name : 'No File Chosen'" />
            </div>
        </div>

        <button type="submit" class="btn-primary-blue-block">Add Employee</button>
    </form>
</div>

@section Scripts {
    <script>
        function calcAge() {
            const dob = document.getElementById('dobInput').value;
            if (!dob) { document.getElementById('ageInput').value = ''; return; }
            const birth = new Date(dob);
            const today = new Date();
            let age = today.getFullYear() - birth.getFullYear();
            const m = today.getMonth() - birth.getMonth();
            if (m < 0 || (m === 0 && today.getDate() < birth.getDate())) age--;
            document.getElementById('ageInput').value = age >= 0 ? age : '';
        }
    </script>
}
